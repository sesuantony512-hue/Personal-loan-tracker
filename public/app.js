const state={page:location.hash.slice(1)||'home',lang:localStorage.getItem('ledgerly-lang')||'en',loans:[],interest:[],borrows:[],tab:'weekly'};
const T={en:{home:'Home',loans:'Loans',interest:'Interest',borrow:'Hand Borrow',settings:'Language',welcome:'Good morning',overview:'Your money overview',loan:'Loan',interestCard:'Interest',borrowCard:'Hand Borrow',loanCount:'active loans',remaining:'remaining to pay',total:'Total',totalAmount:'Total amount',totalDue:'Total due amount',borrowed:'borrowed',addLoan:'+ Add Loan',addInterest:'+ Add Interest',addBorrow:'+ Add Hand Borrow',weekly:'Weekly',monthly:'Monthly',name:'Name',loanName:'Loan name',type:'Type',dueTotal:'Total due',paid:'Paid',pending:'Pending',dueDay:'Due day',dueDate:'Due date',nextDue:'Next due',dueAmount:'Due amount',remainingTotal:'Remaining total',amount:'Amount',borrowedDate:'Borrowed date',actions:'Actions',emptyLoans:'No loans added yet.',emptyInterest:'No interest records yet.',emptyBorrow:'No hand borrows yet.',addFirst:'Add your first record to get started.',save:'Save record',cancel:'Cancel',schedule:'Due amount schedule',from:'From',to:'To',addSchedule:'+ Add schedule',view:'View details',edit:'Edit',payment:'Record payment',payNow:'Pay now',delete:'Delete',confirm:'Are you sure you want to delete this record?',note:'Optional note',paymentDate:'Payment date',amountPaid:'Amount paid',history:'Payment history',noPayments:'No payments recorded.',next:'Next installment',close:'Close',saved:'Record saved.',deleted:'Record deleted.',error:'Something went wrong.',required:'Please complete the required fields.'},ta:{home:'முகப்பு',loans:'கடன்கள்',interest:'வட்டி',borrow:'கை கடன்',settings:'மொழி',welcome:'வணக்கம்',overview:'உங்கள் பண நிலவரம்',loan:'கடன்',interestCard:'வட்டி',borrowCard:'கை கடன்',loanCount:'செயலில் உள்ள கடன்கள்',remaining:'செலுத்த வேண்டியது',total:'மொத்தம்',totalAmount:'மொத்த தொகை',totalDue:'மொத்த நிலுவை',borrowed:'கடன் பெற்றது',addLoan:'+ கடன் சேர்',addInterest:'+ வட்டி சேர்',addBorrow:'+ கை கடன் சேர்',weekly:'வாராந்திரம்',monthly:'மாதாந்திரம்',name:'பெயர்',loanName:'கடன் பெயர்',type:'வகை',dueTotal:'மொத்த தவணைகள்',paid:'செலுத்தியது',pending:'நிலுவை',dueDay:'செலுத்தும் நாள்',dueDate:'செலுத்தும் தேதி',nextDue:'அடுத்த தவணை தேதி',dueAmount:'தவணை தொகை',remainingTotal:'மீதமுள்ள தொகை',amount:'தொகை',borrowedDate:'பெற்ற தேதி',actions:'செயல்கள்',emptyLoans:'இன்னும் கடன்கள் இல்லை.',emptyInterest:'இன்னும் வட்டி பதிவுகள் இல்லை.',emptyBorrow:'இன்னும் கை கடன்கள் இல்லை.',addFirst:'தொடங்க முதல் பதிவைச் சேர்க்கவும்.',save:'பதிவை சேமி',cancel:'ரத்து',schedule:'தவணை தொகை அட்டவணை',from:'முதல்',to:'வரை',addSchedule:'+ அட்டவணை சேர்',view:'விவரங்கள்',edit:'திருத்து',payment:'பணம் செலுத்து',payNow:'இப்போது செலுத்து',delete:'நீக்கு',confirm:'இந்த பதிவை நீக்க விரும்புகிறீர்களா?',note:'விருப்ப குறிப்பு',paymentDate:'செலுத்திய தேதி',amountPaid:'செலுத்திய தொகை',history:'பணம் செலுத்திய வரலாறு',noPayments:'பணம் செலுத்திய பதிவுகள் இல்லை.',next:'அடுத்த தவணை',close:'மூடு',saved:'பதிவு சேமிக்கப்பட்டது.',deleted:'பதிவு நீக்கப்பட்டது.',error:'ஏதோ தவறு ஏற்பட்டது.',required:'தேவையான புலங்களை நிரப்பவும்.'}};
const t=(key)=>T[state.lang][key]||key; const money=(value)=>new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(Number(value||0)); const date=(value)=>value?new Intl.DateTimeFormat('en-IN').format(new Date(value+'T00:00:00')):'-'; const ordinal=(value)=>{const number=Number(value);const suffix=number%100>=11&&number%100<=13?'th':({1:'st',2:'nd',3:'rd'}[number%10]||'th');return `${number}${suffix}`}; const scheduleValue=(loan)=>loan.type==='weekly'?loan.due_day:ordinal(loan.due_date);
async function api(url,options={}){const response=await fetch(url,{headers:{'Content-Type':'application/json'},...options});if(!response.ok)throw new Error((await response.json()).error||t('error'));return response.status===204?null:response.json()}
async function load(){[state.loans,state.interest,state.borrows]=await Promise.all([api('/api/loans'),api('/api/interest'),api('/api/hand-borrow')]);render()}
function nav(){document.querySelector('#nav').innerHTML=[['home','⌂',t('home')],['loans','▣',t('loans')],['interest','◌',t('interest')],['borrow','↗',t('borrow')]].map(([id,icon,label])=>`<a class="nav-item ${state.page===id?'active':''}" href="#${id}"><span>${icon}</span>${label}</a>`).join('');document.querySelector('#language-label').textContent=state.lang==='en'?'English':'தமிழ்'}
function heading(eyebrow,title,desc,action=''){return `<div class="page-heading"><div><p class="eyebrow">${eyebrow}</p><h1>${title}</h1><p>${desc}</p></div>${action}</div>`}
function home(){const remaining=state.loans.reduce((n,l)=>n+l.remaining_total,0);return heading(t('welcome'),t('overview'),'A quiet view of what is moving through your life.')+`<div class="metrics"><a class="metric-link metric" href="#loans"><div class="metric-label">${t('loan')}</div><div class="metric-value">${state.loans.length}</div><div class="muted">${t('loanCount')} · ${money(remaining)} ${t('remaining')}</div></a><a class="metric-link metric" href="#interest"><div class="metric-label">${t('interestCard')}</div><div class="metric-value">${money(state.interest.reduce((n,x)=>n+Number(x.amount),0))}</div><div class="muted">${money(state.interest.reduce((n,x)=>n+Number(x.due_amount),0))} ${t('totalDue')}</div></a><a class="metric-link metric" href="#borrow"><div class="metric-label">${t('borrowCard')}</div><div class="metric-value">${money(state.borrows.reduce((n,x)=>n+Number(x.amount),0))}</div><div class="muted">${t('borrowed')}</div></a></div><h2 class="section-title">${t('loans')}</h2><div class="panel">${state.loans.length?`<div class="table-wrap"><table class="data-table"><tbody>${state.loans.slice(0,4).map(loanRow).join('')}</tbody></table></div>`:`<div class="empty"><strong>${t('emptyLoans')}</strong>${t('addFirst')}</div>`}</div>`}
function loanRow(l){return `<tr><td><strong>${esc(l.name)}</strong><div class="muted">${l.type==='weekly'?t('weekly'):t('monthly')}</div></td><td>${l.total_due}</td><td>${l.paid}</td><td>${l.pending}</td><td>${esc(scheduleValue(l))}</td><td>${date(l.next_due)}</td><td class="money">${money(l.current_due_amount)}</td><td class="money">${money(l.remaining_total)}</td><td class="actions"><button class="ghost" data-action="payment" data-id="${l.id}" ${l.pending===0?'disabled':''}>${t('payNow')}</button><span class="action-menu-wrap"><button class="more" data-menu="loan-${l.id}" aria-label="${t('actions')}">⋮</button>${menu('loan',l.id)}</span></td></tr>`}
function menu(kind,id){return `<div class="menu" id="menu-${kind}-${id}"><button data-action="view" data-id="${id}" data-kind="${kind}">${t('view')}</button><button data-action="edit" data-id="${id}" data-kind="${kind}">${t('edit')}</button>${kind==='loan'?`<button data-action="payment" data-id="${id}" data-kind="${kind}">${t('payment')}</button>`:''}<button class="danger" data-action="delete" data-kind="${kind}" data-id="${id}">${t('delete')}</button></div>`}
function loans(){const filtered=state.loans.filter(l=>l.type===state.tab);return heading(t('loans'),t('loans'),'Weekly and monthly loans, with the real remaining balance.',`<button class="primary" data-action="add-loan">${t('addLoan')}</button>`)+`<div class="tabs"><button class="tab ${state.tab==='weekly'?'active':''}" data-tab="weekly">${t('weekly')} (${state.loans.filter(l=>l.type==='weekly').length})</button><button class="tab ${state.tab==='monthly'?'active':''}" data-tab="monthly">${t('monthly')} (${state.loans.filter(l=>l.type==='monthly').length})</button></div><div class="panel">${filtered.length?`<div class="table-wrap"><table class="data-table"><thead><tr><th>${t('name')}</th><th>${t('dueTotal')}</th><th>${t('paid')}</th><th>${t('pending')}</th><th>${state.tab==='weekly'?t('dueDay'):t('dueDate')}</th><th>${t('nextDue')}</th><th>${t('dueAmount')}</th><th>${t('remainingTotal')}</th><th></th></tr></thead><tbody>${filtered.map(loanRow).join('')}</tbody></table></div><div class="subtle-total"><span>${t('total')}</span><span>${money(filtered.reduce((n,l)=>n+l.remaining_total,0))}</span></div>`:`<div class="empty"><strong>${t('emptyLoans')}</strong>${t('addFirst')}</div>`}</div>`}
function collection(kind,items){const isInterest=kind==='interest';const title=isInterest?t('interest'):t('borrow');return heading(title,title,'Keep the details separate, simple, and useful.',`<button class="primary" data-action="add-${kind}">${isInterest?t('addInterest'):t('addBorrow')}</button>`)+`<div class="panel">${items.length?`<div class="table-wrap"><table class="data-table"><thead><tr><th>${t('name')}</th>${isInterest?`<th>${t('amount')}</th><th>${t('dueDate')}</th><th>${t('dueAmount')}</th>`:`<th>${t('borrowedDate')}</th><th>${t('dueDate')}</th><th>${t('amount')}</th>`}<th></th></tr></thead><tbody>${items.map(x=>`<tr><td><strong>${esc(x.name)}</strong></td>${isInterest?`<td class="money">${money(x.amount)}</td><td>${date(x.due_date)}</td><td class="money">${money(x.due_amount)}</td>`:`<td>${date(x.borrowed_date)}</td><td>${date(x.due_date)}</td><td class="money">${money(x.amount)}</td>`}<td class="actions"><button class="more" data-menu="${kind}-${x.id}">⋮</button>${menu(kind,x.id)}</td></tr>`).join('')}</tbody></table></div><div class="subtle-total"><span>${isInterest?t('totalAmount'):t('total')}</span><span>${money(items.reduce((n,x)=>n+Number(x.amount),0))}</span></div>${isInterest?`<div class="subtle-total"><span>${t('totalDue')}</span><span>${money(items.reduce((n,x)=>n+Number(x.due_amount),0))}</span></div>`:''}`:`<div class="empty"><strong>${isInterest?t('emptyInterest'):t('emptyBorrow')}</strong>${t('addFirst')}</div>`}</div>`}
function esc(value){return String(value).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function render(){nav();document.querySelector('#app').innerHTML=state.page==='home'?home():state.page==='loans'?loans():state.page==='interest'?collection('interest',state.interest):collection('borrow',state.borrows);bindMenus()}
function bindMenus(){
  document.querySelectorAll('[data-menu]').forEach(b=>b.onclick=(event)=>{
    event.stopPropagation();
    const key=b.dataset.menu;
    const m=document.querySelector(`#menu-${key}`);
    if(!m) return;
    const wasOpen = m.classList.contains('open');
    document.querySelectorAll('.menu').forEach(x=>x.classList.remove('open'));
    if(!wasOpen) m.classList.add('open');
  });
  document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{state.tab=b.dataset.tab;render()});
  document.querySelectorAll('[data-action]').forEach(b=>b.onclick=()=>action(b.dataset.action,b.dataset.id,b.dataset.kind));
  document.addEventListener('click',(event)=>{
    if(!event.target.closest('[data-menu]')&&!event.target.closest('.menu')){
      document.querySelectorAll('.menu').forEach(x=>x.classList.remove('open'));
    }
  });
}
function action(kind,id,collectionKind){if(kind==='add-loan')return loanModal();if(kind==='add-interest')return simpleModal('interest');if(kind==='add-borrow')return simpleModal('borrow');if(kind==='view')return kind==='view'&&collectionKind!=='interest'&&collectionKind!=='borrow'?detailModal(id):simpleModal(collectionKind,id);if(kind==='edit')return collectionKind==='interest'||collectionKind==='borrow'?simpleModal(collectionKind,id):loanModal(id);if(kind==='payment')return paymentModal(id);if(kind==='delete')return remove(collectionKind,id)}
function openModal(title,body){document.querySelector('#modal-root').innerHTML=`<div class="modal-backdrop"><div class="modal"><div class="modal-head"><h2>${title}</h2><button class="close" data-close>×</button></div>${body}</div></div>`;document.querySelector('[data-close]').onclick=closeModal;document.querySelector('.modal-backdrop').onclick=e=>{if(e.target.classList.contains('modal-backdrop'))closeModal()}}
function closeModal(){document.querySelector('#modal-root').innerHTML=''}
function loanModal(id){const loan=id?state.loans.find(x=>x.id==id):{name:'',type:'weekly',total_due:'',paid:0,due_day:'Monday',due_date:16,schedule:[{from:1,to:'',amount:''}]};const schedule=loan.schedule||[];openModal(id?t('edit'):t('addLoan'),`<form id="loan-form"><div class="switcher"><label><input type="radio" name="type" value="weekly" ${loan.type==='weekly'?'checked':''}>${t('weekly')}</label><label><input type="radio" name="type" value="monthly" ${loan.type==='monthly'?'checked':''}>${t('monthly')}</label></div><div class="form-grid"><div class="field"><label>${t('loanName')}</label><input name="name" required value="${esc(loan.name)}"></div><div class="field"><label>${t('dueTotal')}</label><input name="totalDue" type="number" min="1" required value="${loan.total_due}"></div><div class="field"><label>${t('paid')}</label><input name="paid" type="number" min="0" required value="${loan.paid}"></div><div class="field" id="frequency-field"></div></div><div class="schedule-head"><strong>${t('schedule')}</strong><button type="button" class="ghost" id="add-schedule">${t('addSchedule')}</button></div><div id="schedule-rows">${schedule.map(scheduleRow).join('')}</div><div id="form-error" class="error"></div><div class="form-actions"><button type="button" class="ghost" data-close>${t('cancel')}</button><button class="primary">${t('save')}</button></div></form>`);const form=document.querySelector('#loan-form');const updateFrequency=()=>{const type=form.type.value;document.querySelector('#frequency-field').innerHTML=type==='weekly'?`<label>${t('dueDay')}</label><select name="dueDay">${['Monday','Tuesday','Wednesday','Thursday','Friday','Saturday','Sunday'].map(x=>`<option ${loan.due_day===x?'selected':''}>${x}</option>`).join('')}</select>`:`<label>${t('dueDate')}</label><input name="dueDate" type="number" min="1" max="31" required value="${loan.due_date||16}">`};form.querySelectorAll('[name=type]').forEach(x=>x.onchange=updateFrequency);updateFrequency();document.querySelector('#add-schedule').onclick=()=>document.querySelector('#schedule-rows').insertAdjacentHTML('beforeend',scheduleRow({from:'',to:'',amount:''}));document.querySelectorAll('[data-close]').forEach(x=>x.onclick=closeModal);form.onsubmit=async e=>{e.preventDefault();const fd=new FormData(form);const payload={name:fd.get('name'),type:fd.get('type'),totalDue:Number(fd.get('totalDue')),paid:Number(fd.get('paid')),dueDay:fd.get('dueDay'),dueDate:Number(fd.get('dueDate')),schedule:[...document.querySelectorAll('.schedule-row')].map(row=>({from:Number(row.querySelector('[name=from]').value),to:Number(row.querySelector('[name=to]').value),amount:Number(row.querySelector('[name=amount]').value)}))};try{await api(id?`/api/loans/${id}`:'/api/loans',{method:id?'PUT':'POST',body:JSON.stringify(payload)});closeModal();toast(t('saved'));await load()}catch(err){document.querySelector('#form-error').textContent=err.message}}}
function scheduleRow(row){return `<div class="schedule-row"><input name="from" type="number" min="1" placeholder="${t('from')}" value="${row.from}"><input name="to" type="number" min="1" placeholder="${t('to')}" value="${row.to}"><input name="amount" type="number" min="0.01" step="0.01" placeholder="${t('dueAmount')}" value="${row.amount}"><button type="button" onclick="this.parentElement.remove()">×</button></div>`}
function simpleModal(kind,id){const item=id?(kind==='interest'?state.interest:state.borrows).find(x=>x.id==id):{};const interest=kind==='interest';openModal(id?t('edit'):interest?t('addInterest'):t('addBorrow'),`<form id="simple-form"><div class="form-grid"><div class="field"><label>${t('name')}</label><input name="name" required value="${esc(item.name||'')}"></div>${interest?`<div class="field"><label>${t('amount')}</label><input name="amount" type="number" min="0.01" step="0.01" required value="${item.amount||''}"></div><div class="field"><label>${t('dueDate')}</label><input name="due_date" type="date" required value="${item.due_date||''}"></div><div class="field"><label>${t('dueAmount')}</label><input name="due_amount" type="number" min="0.01" step="0.01" required value="${item.due_amount||''}"></div>`:`<div class="field"><label>${t('borrowedDate')}</label><input name="borrowed_date" type="date" required value="${item.borrowed_date||''}"></div><div class="field"><label>${t('dueDate')}</label><input name="due_date" type="date" required value="${item.due_date||''}"></div><div class="field"><label>${t('amount')}</label><input name="amount" type="number" min="0.01" step="0.01" required value="${item.amount||''}"></div>`}</div><div id="form-error" class="error"></div><div class="form-actions"><button type="button" class="ghost" data-close>${t('cancel')}</button><button class="primary">${t('save')}</button></div></form>`);document.querySelectorAll('[data-close]').forEach(x=>x.onclick=closeModal);document.querySelector('#simple-form').onsubmit=async e=>{e.preventDefault();const data=Object.fromEntries(new FormData(e.target));['amount','due_amount'].forEach(k=>{if(data[k])data[k]=Number(data[k])});try{await api(`/api/${kind==='interest'?'interest':'hand-borrow'}${id?`/${id}`:''}`,{method:id?'PUT':'POST',body:JSON.stringify(data)});closeModal();toast(t('saved'));await load()}catch(err){document.querySelector('#form-error').textContent=err.message}}}
function detailModal(id){const l=state.loans.find(x=>x.id==id);openModal(t('view'),`<div class="detail-grid">${[['name',l.name],['type',l.type],['dueTotal',l.total_due],['paid',l.paid],['pending',l.pending],[l.type==='weekly'?'dueDay':'dueDate',scheduleValue(l)],['nextDue',date(l.next_due)],['next',l.next_installment||'-'],['dueAmount',money(l.current_due_amount)],['remainingTotal',money(l.remaining_total)]].map(([k,v])=>`<div class="detail"><span>${t(k)}</span><strong>${esc(v)}</strong></div>`).join('')}</div><h3 class="section-title">${t('schedule')}</h3><div class="table-wrap"><table class="data-table"><tbody>${l.schedule.map(x=>`<tr><td>${x.from}</td><td>${x.to}</td><td class="money">${money(x.amount)}</td></tr>`).join('')}</tbody></table></div><h3 class="section-title">${t('history')}</h3><div class="table-wrap"><table class="data-table"><tbody>${l.payments?.length?l.payments.map(x=>`<tr><td>${date(x.payment_date)}</td><td>#${x.installment_number}</td><td>${money(x.amount_paid)}</td><td>${esc(x.note||'')}</td></tr>`).join(''):`<tr><td class="empty">${t('noPayments')}</td></tr>`}</tbody></table></div>`);}
function paymentModal(id){const l=state.loans.find(x=>x.id==id);openModal(t('payment'),`<form id="payment-form"><p class="muted">${t('next')}: <strong>${l.next_installment||'-'}</strong> · ${money(l.current_due_amount)}</p><div class="form-grid"><div class="field"><label>${t('amountPaid')}</label><input name="amountPaid" type="number" min="0.01" step="0.01" required value="${l.current_due_amount}"></div><div class="field"><label>${t('paymentDate')}</label><input name="paymentDate" type="date" required value="${new Date().toISOString().slice(0,10)}"></div><div class="field full"><label>${t('note')}</label><textarea name="note"></textarea></div></div><div id="form-error" class="error"></div><div class="form-actions"><button type="button" class="ghost" data-close>${t('cancel')}</button><button class="primary">${t('save')}</button></div></form>`);document.querySelectorAll('[data-close]').forEach(x=>x.onclick=closeModal);document.querySelector('#payment-form').onsubmit=async e=>{e.preventDefault();try{await api(`/api/loans/${id}/payments`,{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(e.target)))});closeModal();toast(t('saved'));await load()}catch(err){document.querySelector('#form-error').textContent=err.message}}}
async function remove(kind,id){if(!confirm(t('confirm')))return;try{await api(`/api/${kind==='loan'?'loans':kind==='interest'?'interest':'hand-borrow'}/${id}`,{method:'DELETE'});toast(t('deleted'));await load()}catch(err){toast(err.message)}}function toast(message){const x=document.querySelector('#toast');x.textContent=message;x.classList.add('show');setTimeout(()=>x.classList.remove('show'),2200)}
document.querySelector('#language-button').onclick=()=>{state.lang=state.lang==='en'?'ta':'en';localStorage.setItem('ledgerly-lang',state.lang);render()};document.querySelector('#mobile-menu').onclick=()=>document.querySelector('.sidebar').classList.toggle('open');window.onhashchange=()=>{state.page=location.hash.slice(1)||'home';render()};load().catch(()=>toast(t('error')));